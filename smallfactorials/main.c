#include <stdio.h>
#include <stdlib.h>

int fact(x);
int main()
{
    int t;
    int i;
    int n;
    long x;
    scanf("%d",t);
    int a[i];





    int fact()
    {
        for(i=0;i<t;i++)
    {
        scanf("%d",&n);
        a[i]=n;
    }

        for(i=a[i];i<=1;i++)
        {
            a[i]=a[i]*(a[i]-1);
        }
        return a[i];
        }
        for(i=0;i<t;i++)
    {
        printf(fact());
    }
    }




